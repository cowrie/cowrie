# legacy-debian-7

This is the current default Cowrie surface — Debian 7 wheezy, kernel 3.2
from 2014. Selecting this flavor produces the same image as building
without `FLAVOR`. It exists so deployments can pin the legacy fingerprint
explicitly rather than implicitly relying on whatever the upstream default
happens to be.

Intentionally empty — no overrides needed.
